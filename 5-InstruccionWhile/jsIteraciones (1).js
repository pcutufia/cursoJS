function mostrar()
{
	var contador = 0;

	while( contador < 10) {

	console.log("Cont:" + contador);

	contador++;
}	
console.log("Afuera cont: " + contador);


// con -- va decreciendo y ++ incrementando
// if( contador == 4 { break; }) salis en el contador nº 4
// continue: deja de hacer esa iteracion y pasa a la siguiente. Evalua la condiciòn.
// if ( contador % 2) {continue; }  console.log(contador);} 

// while simpre vuelve a iniciar (repetitiva), no como el if que pasa de largo despues de validar.
}//FIN DE LA FUNCIÓN